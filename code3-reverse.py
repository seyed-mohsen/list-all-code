#عددی سه رقمی را دریافت میکند سپس آن عدد را معکوس میکند و دو برابر آن را پرینت میکند 
asl =(int( input ()))
aks = (str(asl %10))+(str((asl // 10)%10))+(str(asl //100))
has = (int (aks ))*2
print (has)
